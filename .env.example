# Prefer OVS_SESSION_FILE. Never commit the referenced file.
OVS_SESSION_FILE=/absolute/path/to/session.json

# Optional server settings for `ovs-mcp --transport http`.
OVS_MCP_HOST=127.0.0.1
OVS_MCP_PORT=3000
